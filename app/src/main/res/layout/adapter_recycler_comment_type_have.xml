<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingTop="5dp"
    android:paddingLeft="5dp"
    android:paddingRight="5dp">

    <ImageView
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:src="@mipmap/ic_launcher"
        android:id="@+id/adapter_comment_type_have_image"
        android:layout_marginTop="5dp"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/adapter_comment_type_have_image"
        android:layout_marginStart="10dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="name"
            android:textSize="14sp"
            android:id="@+id/adapter_comment_type_have_name"
            android:textColor="@color/adapter_recycler_comment_name_text_color"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/adapter_comment_type_have_name"
            android:textSize="12sp"
            tools:text="date"
            android:layout_marginTop="2dp"
            android:id="@+id/adapter_comment_type_have_date"
            android:textColor="@color/adapter_recycler_comment_date_text_color"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/adapter_comment_type_have_date"
            android:id="@+id/adapter_comment_type_have_comment"
            android:textSize="14sp"
            android:textColor="@color/adapter_recycler_comment_comment_text_color"
            tools:text="comment"
            android:layout_marginTop="5dp"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/adapter_recycler_comment_text_background"
            android:layout_marginTop="2dp"
            android:layout_below="@id/adapter_comment_type_have_comment"/>
    </RelativeLayout>

</RelativeLayout>