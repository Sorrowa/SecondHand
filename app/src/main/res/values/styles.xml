<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="TransparentTheme" parent="AppTheme">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
    <style name="WindowsTransparent" parent="AppTheme">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!--男色按钮-->
    <style name="Button.Blue" parent="">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/selector_button_blue</item>
    </style>

    <!--评价星级-->
    <style name="mRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:minHeight">38dip</item>
        <item name="android:progressDrawable">@drawable/rate_star_big_on</item>
        <item name="android:maxHeight">38dip</item>
    </style>
    <!--<style name="mRatingBar">-->
        <!--<item name="android:indeterminateOnly">false</item>-->
        <!--<item name="android:progressDrawable">@drawable/ratingbar</item>-->
        <!--<item name="android:indeterminateDrawable">@drawable/ratingbar</item>-->
        <!--<item name="android:minHeight">38dip</item>-->
        <!--<item name="android:maxHeight">38dip</item>-->
        <!--<item name="android:thumb">@null</item>-->
        <!--<item name="android:isIndicator">true</item>-->
    <!--</style>-->


</resources>
